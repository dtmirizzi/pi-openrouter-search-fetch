/**
 * @dtmirizzi/pi-openrouter-multimodal
 *
 * OpenRouter multimodal tool extensions for Pi.
 * Re-exports the extension from src/ so Pi registers it with the package name.
 */
export { default } from "./src/index";
